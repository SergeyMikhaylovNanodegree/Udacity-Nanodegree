<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="projects_buttons" parent="android:Widget.Material.Button">
        <item name="android:colorPressedHighlight">@color/projects_names_btn_pressed</item>
        <item name="android:colorButtonNormal">@color/projects_names_btn_background</item>
        <item name="android:textColor">@color/projects_names_btn_text_color</item>
        <item name="android:width">220dp</item>

    </style>

</resources>
